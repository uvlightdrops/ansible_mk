---
# Ansible Galaxy collections
# Usage (nach pip install):
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"

