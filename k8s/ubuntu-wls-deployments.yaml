
# DEPRECATED: this monolithic manifest has been split into modular manifests under k8s/
# See: deploy-wls-admin.yaml, deploy-wls-managed-1.yaml, deploy-wls-managed-2.yaml,
# deploy-test-db.yaml, services-clusterip.yaml and services-nodeports.yaml
#
# The full original content was preserved in `k8s/ubuntu-wls-deployments.deprecated.yaml`.
# Apply the split manifests instead to avoid duplication and easier maintenance.

