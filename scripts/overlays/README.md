# Overlay-specific scripts

- `scripts/overlays/hosted/preflight_check.sh`
  - Wrapper for hosted/operator preflight validation.
  - Delegates to `scripts/preflight_check.sh`.

Use overlay-scoped paths in docs and automation to avoid confusion between hosted/minikube/manual workflows.

