apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-weblogic-home
  namespace: weblogic
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: manual

