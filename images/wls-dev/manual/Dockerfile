FROM wls-dev:base

# Manual/hosted overlay: no SSH, no entrypoint.
# Starts clean as non-root (UID 1000 = docker user).
# command/args come entirely from the Kubernetes manifest.

CMD ["sleep", "infinity"]
