# Operator manifest placeholder
#
# The old release asset URL used in older docs no longer exists.
# Generate this file with:
#   ./scripts/render_weblogic_operator_manifest.sh
#
# The generated file combines:
#   - domain-crd.yaml
#   - cluster-crd.yaml
#   - the rendered Helm chart for the WebLogic Operator
#
# After generating this file, the playbook can apply it.

# (This file is intentionally left as a placeholder until generated.)

