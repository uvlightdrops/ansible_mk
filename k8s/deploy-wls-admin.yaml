apiVersion: apps/v1
kind: Deployment
metadata:
  name: wls-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wls-admin
  template:
    metadata:
      labels:
        app: wls-admin
    spec:
      volumes:
      - name: domain-home
        persistentVolumeClaim:
          claimName: pvc-weblogic-home

      containers:
      - name: wls-admin
        image: wls-dev:1.3
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          seccompProfile:
            type: RuntimeDefault
        # Keep pod alive for kubectl/Ansible exec workflows without SSH.
        args: ["sleep", "infinity"]
        volumeMounts:
        - name: domain-home
          mountPath: /u01/domains

