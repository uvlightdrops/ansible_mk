apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: wls-managed-3
spec:
  replicas: 1
  serviceName: wls-managed-3-headless
  selector:
    matchLabels:
      app: wls-managed-3
  template:
    metadata:
      labels:
        app: wls-managed-3
    spec:
      imagePullSecrets:
      - name: wls-image-secret
      volumes:
      - name: domain-home
        persistentVolumeClaim:
          claimName: pvc-weblogic-home

      containers:
      - name: wls-managed
        image: wls-dev:1.3
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          seccompProfile:
            type: RuntimeDefault
        # Override image entrypoint to avoid startup scripts that require chown/root.
        command: ["sleep"]
        args: ["infinity]
        volumeMounts:
        - name: domain-home
          mountPath: /u01/domains

