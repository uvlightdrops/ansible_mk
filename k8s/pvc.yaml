# DEPRECATED: duplicate of pvc-weblogic-home definition. Use k8s/pvc-weblogic-home.yaml instead.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-weblogic-home-deprecated
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: manual

