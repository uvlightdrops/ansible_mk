[defaults]
# The inventory file in the repo root (generated by scripts)
inventory = ../inventory.yaml
host_key_checking = False
retry_files_enabled = False
# default private key (override with environment variable or CLI if needed)
private_key_file = ~/.ssh/id_ed25519
timeout = 10

